/*
 * problem.c
 *
 *  Created on: Jun 4, 2018
 *      Author: aivan
 */

#include <string.h>

#include "problem.h"

double E(struct graph *G, long idx, const double *x, double *e) {

	memset(e, 0, sizeof(G->dim * sizeof(unsigned char)));

	double z = 0.0;

	return z;

}

void fdf(struct graph *G, const double *x, double *f, double *g) {

	*f = 0.0;

	for (long i = 0; i < G->nnodes; i++) {

	}

}

